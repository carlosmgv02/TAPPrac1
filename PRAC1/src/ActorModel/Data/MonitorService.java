package ActorModel.Data;

import ActorModel.Data.Messages.Message;

import java.util.HashMap;
import java.util.Map;
import java.util.Queue;

public class MonitorService {
    /*
    //TODO
    //tenemos que crear un ActorListener que nos informe de la (creación, finalización, error y mensaje recibido) de un actor
    //tenemos que crear un método monitorActor(name) y otro monitorAllActors()

    //tenemos que crear un método getEvents que retorne un Map donde la key sea CREATED, STOPPED O ERROR y el valor conetenga los evantos tratados anteriormente


    public void assignTraffic(){ //no está bién
       if (Actor.getQueLength() < 5){
           Actor.traffic = "LOW";
       } else if (Actor.getQueLength() > 5 && Actor.getQueLength() < 10){
           Actor.traffic = "MEDIUM";
       } else if (Actor.getQueLength() > 15){
           Actor.traffic = "HIG";
       }
       }
    }

    public getTraffic(){ //no esta acabado
        Map<String, Actor> traffic = new HashMap<>();

        traffic.put(Actor.id, Actor.traffic);

    }


    @Override
    public void send(Message msg) {

    }

    @Override
    public Message process() {
        return null;
    }

    @Override
    public int getQueLength() {
        return 0;
    }

    @Override
    public Queue<Message> getQueue() {
        return null;
    }

    @Override
    public void run() {

    }

    @Override
    public void start() {

    }*/
}
